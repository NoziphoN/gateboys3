@model IEnumerable<GateBoys.Models.Order>
@using Microsoft.AspNet.Identity

@{
    ViewBag.Title = "Index";
}

@{
    string user = User.Identity.GetUserName();
    string name = user.Split('@')[0];
}
<br />
<h2>My Orders</h2>

<ul class="nav nav-tabs">


    <li class="active"><a data-toggle="tab" href="#paid">Paid for</a></li>

    <li><a data-toggle="tab" href="#notpaid">Not Paid </a></li>


</ul>
<div class="tab-content">
    <div id="paid" class="tab-pane fade in active">
        <br />
        <a href="/orders/printOrders?status=Paid">Print All <span class="glyphicon glyphicon-download-alt"></span></a>

        <br />
        <table class="timetable_sub">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderQuantity)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.TotalOrderCost)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Status)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.orderedItems)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DeliveryAddress)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.username)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Cell)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AssignedDriver)
                    </th>
                    <th></th>
                </tr>
            </thead>

            @foreach (var item in Model.Where(a=>a.username== user))
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.OrderNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OrderDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OrderQuantity)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TotalOrderCost)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Status)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.orderedItems)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DeliveryAddress)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.username)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cell)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AssignedDriver)
                    </td>
                    <td>
                        <a href="/orders/printOrder/@item.OrderId"><span class="glyphicon glyphicon-download-alt"></span></a>
                        |
                        @Html.ActionLink("Details", "Details", new { id = item.OrderId })
                        @*@Html.ActionLink("Delete", "Delete", new { id = item.OrderId })*@
                    </td>
                </tr>
            }

        </table>

    </div>
    <div id="notpaid" class="tab-pane fade in">
        <br />
        <a href="/orders/printOrders?status=notpaid">Print All <span class="glyphicon glyphicon-download-alt"></span></a>
        <br />
        <table class="timetable_sub">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderNumber)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderQuantity)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.TotalOrderCost)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Status)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.orderedItems)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DeliveryAddress)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.username)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Cell)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AssignedDriver)
                    </th>
                    <th></th>
                </tr>
            </thead>

            @foreach (var item in Model.Where(a=>a.username== user))
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.OrderNumber)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OrderDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OrderQuantity)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TotalOrderCost)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Status)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.orderedItems)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DeliveryAddress)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.username)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Cell)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AssignedDriver)
                    </td>
                    <td>
                        <a href="/orders/printOrder/@item.OrderId"><span class="glyphicon glyphicon-download-alt"></span></a>
                      
                    </td>
                </tr>
            }

        </table>

    </div>
</div>
